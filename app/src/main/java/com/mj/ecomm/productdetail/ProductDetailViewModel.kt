package com.mj.ecomm.productdetail

import com.mj.ecomm.base.BaseViewModel
import com.mj.ecomm.data.ProductDetailRepository

class ProductDetailViewModel(private val productDetailRepository: ProductDetailRepository) : BaseViewModel(productDetailRepository) {
}